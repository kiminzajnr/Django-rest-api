# Build an API with Django Rest Famework




Run the following commands to get started:

```json
virtualenv env
pip install -r requirements.txt
python manage.py runserver


To access admin:
python manage.py createsuperuser
```